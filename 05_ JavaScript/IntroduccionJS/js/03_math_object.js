// MATH OBJECT

let result;

result = Math.PI;
result = Math.round(2.5); // Round
result = Math.ceil(2.3); // Round to up
result = Math.floor(2.8); // Round to down
result = Math.sqrt(144); // Square root
result = Math.abs(-200); // Absolute
result = Math.min(3, 5, 8, 10); // Minimun
result = Math.max(3, 5, 8, 10); // Maximun
result = Math.random(); // Random
result = Math.floor(Math.random() * 30); // Random between 0 and 30

console.log(result);